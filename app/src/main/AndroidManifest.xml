<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.lennon.navigationapp">

    <application
            android:theme="@style/AppTheme"
            android:name=".MyApplication"
            android:allowBackup="true"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:screenOrientation="portrait"
            android:supportsRtl="true">

        <activity android:windowSoftInputMode="stateHidden"
                  android:name=".view.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:theme="@style/AppTheme.Onboarding"
                android:name=".view.onboarding.OnboardingActivity"/>

        <activity android:windowSoftInputMode="stateHidden"
                  android:name=".view.onboarding.SignCompleteActivity"/>

        <activity android:windowSoftInputMode="stateHidden"
                  android:name=".view.onboarding.HomeActivity"/>

    </application>
</manifest>
